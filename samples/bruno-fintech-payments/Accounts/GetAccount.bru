meta {
  name: Get Account
  type: http
  seq: 2
}

get {
  url: {{base-url}}/accounts/:accountId
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{session-access-token}}
}

params:path {
  accountId: {{primary-account-id}}
}
