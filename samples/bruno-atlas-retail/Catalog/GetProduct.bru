meta {
  name: Get Product
  type: http
  seq: 2
}

get {
  url: {{base-url}}/catalog/products/:productId
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{session-access-token}}
}

params:path {
  productId: {{featured-product-id}}
}
