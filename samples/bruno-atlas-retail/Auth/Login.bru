meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{base-url}}/auth/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  X-Request-Id: {{request-id}}
}

body:json {
  {
    "email": "{{auth-email}}",
    "password": "{{auth-password}}",
    "tenant": "{{tenant-id}}"
  }
}
